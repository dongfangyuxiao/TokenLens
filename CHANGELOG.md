# 更新日志

## 2026-03-30

### 📌 今日同步摘要

- 新增多模型交叉审计链：手动触发、计划任务、历史重跑均支持 `llm_profile_ids`
- 新增四种复核策略：`single / any / majority / all`
- `analyzer.py` 支持多模型逐文件复核与跨文件复核，并按共识策略合并审计结果
- 新增 adaptive skills 自学习：多模型共识确认的结果会按语言和任务沉淀，并在后续 prompt 中自动增强
- 新增负向 adaptive skills：`majority / all` 下被多数模型否决的低可信模式也会沉淀，用于后续自动压制误报
- AI 配置表单新增“启用多模型联合校验后自动优化 skills”勾选项，开启后该模型参与联合校验时会自动调整 skills
- `scan_schedules` 与 `scans` 表新增 `llm_profile_ids`、`llm_consensus_mode` 字段，保留旧版 `llm_profile_id` 兼容
- 新增 `adaptive_skills` 表与 `/api/adaptive-skills` 接口，用于查看自动优化后的 skills
- 系统内置新增 `Java / PHP / Python / Go / Node.js / C# / Ruby / Rust / C/C++` 等常见语言专项 skills / prompts
- 参考 DVWA 官方漏洞模块目录补齐检查项，对齐 `api`、`authbypass`、`bac`、`cryptography`、`csp`、`open_redirect`、`weak_id` 等场景
- 设置页与扫描触发弹窗新增多模型链配置与复核策略选择
- 后端提示词补齐 open redirect、weak identifier 等易漏报项

## 2026-03-27

### 📌 今日同步摘要

- 即时分析（粘贴代码 / 上传文件）新增 AI 模型选择，支持按 `llm_profile_id` 指定模型执行
- 修复即时分析上传鉴权问题（统一使用 `audit_token`），避免误触发“未授权请登录”
- 扫描流程改为仓库级流式处理：同账号下按“拉一个仓库，扫一个仓库”执行，并实时刷新漏洞入库
- 扫描状态新增实时进度百分比（`/api/status.progress`），前端顶部状态与概览状态同步显示
- 扫描历史支持运行中/暂停中查看“实时漏洞”，弹窗自动轮询刷新
- 代码库配置页新增“仓库列表”（位于账户列表下方），支持刷新与展示同步状态
- 全量审计后自动清理失效仓库（远端已不存在的仓库从列表中移除），并联动清理对应组件清单
- 应急排查结果改为分页展示，支持每页 20/50/100 切换，避免超长页面
- Syslog facility 改为可多选，并增加中文说明标签（如“应用日志（local0）”）
- 系统设置新增“配置文件检测”开关（默认关闭）；关闭时配置文件不送检，开启后纳入检测
- 新增 Docker 部署支持：提供 `Dockerfile`、`docker-compose.yml`、`.dockerignore`，并支持 `DB_PATH/REPORTS_DIR/SYNC_ROOT` 路径配置
- 增加源码防泄漏与防抄袭基础能力：`LICENSE`、`SECURITY.md`、`pre-commit` 密钥拦截脚本、交付保护文档
- 授权功能增强：系统设置支持上传授权文件、下载机器码文件、按机器码在线生成授权文件并下载

## 2026-03-26

### 📌 今日同步摘要

- LLM 配置统一为新版字段（`provider/model/api_key/base_url`），前后端已移除旧版 `deepseek_api_key` / `anthropic_api_key` 展示与保存入口
- AI 配置支持本地模型与云端模型统一多模型管理，扫描选择器显示配置名 + 提供商/模型
- 投毒检测、增量审计、全量审计均支持手动触发和计划任务绑定独立模型；修复扫描记录未写入 `llm_profile_id` 的问题
- 全量审计新增仓库主分支快照同步：每次全量扫描会将 main/master 代码同步到本地 `synced_repos/`
- 新增软件成分分析（SCA）清单入库与查询能力，支持按组件、仓库、来源过滤
- 新增应急排查能力：支持恶意依赖全仓命中检测，支持全代码库关键词模糊检索（如 `swagger.html`）
- 新增产品授权能力，支持授权码生成、实例绑定、后台录入、状态校验与临期提醒
- 授权支持按功能点控制 `poison_scan`、`incremental_audit`、`full_audit`、`instant_analysis`
- 系统设置补充授权配置入口，界面新增顶部授权状态胶囊和右下角授权水印
- 修复管理员删除与空结果导出等边界问题
- 前端完成科技安全控制台风格重构，并恢复完整的 Light / Dark 双主题
- 补齐浅色模式下代码库配置、扫描历史、漏洞统计、提示词管理、账户管理、系统设置等页面样式
- README、技术文档、使用手册、更新日志已同步更新到当前实现
### 🔐 产品授权能力

- 新增 `license_manager.py`，支持基于 HMAC-SHA256 的授权码签发与校验
- 新增实例 ID 机制，可生成绑定到指定部署实例的授权码
- 系统设置新增“产品授权”卡片，可查看授权状态、录入授权码、切换授权校验开关
- 新增 `/api/license-status` 与 `/api/license-config` 接口
- 新增授权临期提醒、顶部授权状态胶囊和右下角授权信息水印
- 授权可按功能点限制 `poison_scan`、`incremental_audit`、`full_audit`、`instant_analysis`
- 默认不启用授权拦截，避免影响现网使用；启用后会校验扫描与即时分析相关接口
- README、技术文档、用户手册同步补充授权生成与校验说明

### 🔧 稳定性修复与 UI 重构

- 修复启动时会无条件删除 `light` 管理员账号的硬编码逻辑
- 修复删除管理员时“用户不存在”和“至少保留一个管理员账户”错误提示混淆的问题
- 修复无漏洞扫描记录无法导出 JSON / Markdown / DOCX 的可用性问题
- 前端整体视觉从浅色后台风格升级为深色安全控制台风格，默认主题调整为深色
- 补充平板和手机场景下的导航、统计卡片、弹窗等响应式布局
- 二次收紧视觉风格，降低登录页粉色和主界面绿色占比，强化左侧栏发光导轨、芯片式图标和激活态层次
- 更新技术文档、用户手册与项目说明，校正文档中和现网实现不一致的接口、菜单与表结构描述

### ✨ 新功能

#### 跨文件联合安全分析（二次分析）

在原有逐文件并发分析的基础上，新增跨文件第二轮联合分析，能够发现单独看任意一个文件都察觉不到的漏洞。

**工作机制**

```
第一轮（并发）             第二轮（串行）
──────────────             ──────────────
file_a.py → LLM ──┐
file_b.py → LLM ──┼──→ 汇总摘要 + 代码片段 + 已发现漏洞 ──→ LLM ──→ 跨文件漏洞
file_c.py → LLM ──┘
```

**触发条件**
- 同一仓库/提交包含 2 个或以上文件
- 已配置 LLM（无 LLM 时跳过）

**专门识别的跨文件风险类型**

| 类型 | 示例 |
|---|---|
| 数据流跨文件 | 用户输入在 `routes.py` 进入，未经过滤直接到达 `db.py` 中的 SQL 执行点 |
| 鉴权缺口 | 路由定义在文件 A，鉴权中间件在文件 B，二者之间存在可绕过的路径 |
| 配置联动 | `config.py` 关闭了 SSL 验证，`client.py` 中的调用因此可被中间人攻击 |
| 协同修改 | 攻击者同时修改校验函数和调用处，绕过安全检查 |
| 跨文件提权链 | 低权限操作经由多个文件传递，最终触达高权限函数 |

**Token 用量控制**
- 每个文件的代码片段截取前 1600 字符
- 超过 15 个文件时，多余文件仅附文件摘要、不附代码片段
- 要求 LLM 不重复报告第一轮已发现的问题

**涉及文件**
- `analyzer.py`：新增 `_analyze_cross_file()` 函数；`_analyze_single_file()` 额外返回文件摘要；`analyze_commit()` 在第一轮结束后调用第二轮
- `database.py`：`findings` 表新增 `is_cross_file`（0/1）和 `cross_files`（JSON 数组）两列，含自动迁移

**UI 展示**

跨文件漏洞在漏洞详情弹窗中显示紫色 **🔗 跨文件** 标签，并以标签形式列出所有涉及的文件，而非单一文件路径。

---

#### 扫描触发弹窗（选 AI 模型）

点击顶栏"投毒检测 / 增量审计 / 全量审计"按钮后，弹出模态窗口，而非直接触发扫描。

弹窗内容：
- 扫描类型的图标、名称、功能描述（支持中英双语）
- AI 模型选择器（可选"全局 AI 配置"或任意已配置的 AI 配置列表）
- 取消 / 开始扫描按钮

移除了之前顶栏的 LLM 下拉框，交互更清晰。

---

#### 报告导出：DOCX 和 PDF 格式

扫描历史表格新增两个导出按钮：

| 按钮 | 实现方式 |
|---|---|
| **PDF** | 在新标签页打开 HTML 报告，自动触发浏览器打印对话框，另存为 PDF，完整支持中文，无需额外字体库 |
| **DOCX** | 后端用 `python-docx` 生成结构化 Word 文档，含封面信息、按仓库分组的漏洞列表、严重程度颜色标注，原生支持中文 |

DOCX 导出接口：`GET /api/scans/{id}/export?format=docx`

---

#### 深色 / 浅色主题切换

顶栏新增 🌙 / ☀️ 切换按钮。

- 深色模式覆盖全部主要 UI 元素：侧边栏、顶栏、卡片、表格、输入框、弹窗、日志区域等
- 偏好自动存入 `localStorage`，刷新后保持

---

#### 中文 / 英文界面切换

顶栏新增"中 / EN"切换按钮，支持以下元素的双语切换：

- 导航菜单项
- 页面标题
- 顶栏扫描按钮
- 扫描触发弹窗内容（含每种扫描类型的描述文字）
- 扫描状态标签（完成 / 进行中 / 已暂停 / 已中断等）
- AI 模型选择器占位文字

偏好自动存入 `localStorage`，刷新后保持。

---

### 🐛 修复 / 优化

- `_analyze_single_file()` 返回值从 `list` 改为 `(findings, summary)` 元组，为跨文件分析收集文件摘要提供基础
- `save_findings()` 更新 INSERT 语句，保存 `is_cross_file` 和 `cross_files` 字段
- 数据库迁移：已有数据库启动时自动追加 `is_cross_file` 和 `cross_files` 列，无需手动变更

---

### 📦 依赖变更

| 包 | 用途 |
|---|---|
| `python-docx` | DOCX 报告导出（已安装） |
| `reportlab` | PDF 生成（已安装，本次未直接使用，备用） |
| `fpdf2` | PDF 生成（已安装，本次未直接使用，备用） |
